import type { I18nConfig } from './types'

/**
 * Define an i18n configuration with type checking.
 * This is a simple identity function for type safety and discoverability.
 */
export function defineI18nConfig(config: I18nConfig): I18nConfig {
  return config
}
